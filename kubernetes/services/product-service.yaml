apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: devnexus
  labels:
    app: product-service
    project: devnexus
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
