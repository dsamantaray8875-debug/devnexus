apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: devnexus
  labels:
    app: nginx
    project: devnexus
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
