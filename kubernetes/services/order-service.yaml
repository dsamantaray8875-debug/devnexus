apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: devnexus
  labels:
    app: order-service
    project: devnexus
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
